# Pipeline principal (Bronze → Silver → Gold)
pandas>=2.0.0
pyarrow>=14.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Testes
pytest>=7.0.0

# Legado (estrutura original — não utilizado pelo pipeline pandas)
# pyspark>=3.4.0
# delta-spark>=3.0.0